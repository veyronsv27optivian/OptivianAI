/**
 * AI Logger barrel.
 *
 * Every AI request is logged with: request id, provider, feature,
 * execution time, status, error, and retry count.
 * Logs are reusable for an Admin Dashboard later.
 */

export { AiLogger } from './aiLogger';
